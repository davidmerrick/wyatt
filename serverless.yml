service: wyatt

provider:
  name: aws
  runtime: java8
  stage: ${opt:stage, "dev"}
  region: us-west-2
  memorySize: 512
  timeout: 20

package:
  artifact: build/libs/${self:service}.jar

# Todo: Add a CloudWatch event source here
functions:
  helloworld-handler:
    handler: com.merricklabs.wyatt.handlers.WyattHandler